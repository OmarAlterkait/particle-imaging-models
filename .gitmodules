[submodule "libs/pimm-data"]
	path = libs/pimm-data
	url = git@github.com:OmarAlterkait/pimm-data.git
	# Pinned by the recorded gitlink SHA only — no `branch =` line, so a
	# `git submodule update --remote` cannot silently drift off the pinned commit.
